{
  "name": "passkey-sdk",
  "private": true,
  "version": "0.0.0",
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "test:parity": "cd tests/parity/runner && pnpm test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
